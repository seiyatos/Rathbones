# Prometheus config for scraping Investor ML API metrics.
# Used by docker-compose; scrape interval 15s.

scrape_configs:
  - job_name: investor-ml
    static_configs:
      - targets: ["investor-ml:8000"]
    metrics_path: /metrics
    scrape_interval: 15s
    scrape_timeout: 10s
