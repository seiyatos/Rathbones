# Feast feature store config (paths relative to repo root or absolute).
# Override online_store.path via env or config for production (e.g. Redis).
project: investor_ml
registry: data/registry.db
provider: local
entity_key_serialization_version: 2

offline_store:
  type: file

online_store:
  type: sqlite
  path: data/online_store.db
